import { HighlightMenuDirective } from './highlight-menu.directive';

describe('HighlightMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMenuDirective();
    expect(directive).toBeTruthy();
  });
});
